<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zm.mapper.WfFactTaskcountMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zm.model.WfFactTaskcount">
        <id column="ITMNO" property="itmno" />
        <result column="MONTH" property="month" />
        <result column="PARTICIPANTID" property="participantid" />
        <result column="STARTNUM" property="startnum" />
        <result column="FINISHEDNUM" property="finishednum" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        ITMNO AS itmno, MONTH AS month, PARTICIPANTID AS participantid, STARTNUM AS startnum, FINISHEDNUM AS finishednum
    </sql>

</mapper>
