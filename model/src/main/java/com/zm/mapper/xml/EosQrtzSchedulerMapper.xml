<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zm.mapper.EosQrtzSchedulerMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zm.model.EosQrtzScheduler">
        <id column="UUID" property="uuid" />
        <result column="SCHEDULER" property="scheduler" />
        <result column="NODE" property="node" />
        <result column="STATUS" property="status" />
        <result column="HEARTBEATTIME" property="heartbeattime" />
        <result column="VALIDATEFLAG" property="validateflag" />
        <result column="VALIDATETIME" property="validatetime" />
        <result column="INVALIDATETIME" property="invalidatetime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        UUID AS uuid, SCHEDULER AS scheduler, NODE AS node, STATUS AS status, HEARTBEATTIME AS heartbeattime, VALIDATEFLAG AS validateflag, VALIDATETIME AS validatetime, INVALIDATETIME AS invalidatetime
    </sql>

</mapper>
